import 'package:flutter/material.dart';

class NewRegistrationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Registrations'),
      ),
      body: Center(
        child: Text('New Registrations Page Content'),
      ),
    );
  }
}
