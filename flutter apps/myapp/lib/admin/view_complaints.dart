import 'package:flutter/material.dart';

class ViewComplaintsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Complaints'),
      ),
      body: Center(
        child: Text('Complaints will be displayed here'),
      ),
    );
  }
}