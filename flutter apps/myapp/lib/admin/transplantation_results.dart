import 'package:flutter/material.dart';

class TransplantationResultsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transplantation Results'),
      ),
      body: Center(
        child: Text('Transplantation Results Page Content'),
      ),
    );
  }
}
