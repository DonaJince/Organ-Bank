import 'package:flutter/material.dart';

class ViewFeedbacksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Feedbacks'),
      ),
      body: Center(
        child: Text('View Feedbacks Page Content'),
      ),
    );
  }
}
