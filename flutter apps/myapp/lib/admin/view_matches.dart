import 'package:flutter/material.dart';

class ViewMatchesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Matches'),
      ),
      body: Center(
        child: Text('Matches will be displayed here'),
      ),
    );
  }
}